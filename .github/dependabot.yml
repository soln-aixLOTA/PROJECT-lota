<<<<<<< HEAD
# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for more information:
# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates
# https://containers.dev/guide/dependabot

version: 2
updates:
 - package-ecosystem: "devcontainers"
   directory: "/"
   schedule:
     interval: weekly
=======
# Dependabot configuration for automated dependency updates
# Documentation: https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  # Update for JavaScript packages using npm
  - package-ecosystem: "npm" # Specify the package manager (e.g., npm, pip)
    directory: "/" # Location of package manifests
    schedule:
      interval: "weekly" # Frequency of update checks
    open-pull-requests-limit: 5 # Maximum number of open PRs at a time
    commit-message:
      prefix: "deps" # Prefix for commit messages
    # Automated security updates
    allow:
      - dependency-type: "direct"
      - dependency-type: "indirect"
    # Ignore specific dependencies if needed
    # ignore:
    #   - dependency-name: "react"
    #     versions:
    #       - "16.x.x"

  # Update for Python packages using pip
  - package-ecosystem: "pip" 
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    commit-message:
      prefix: "deps"
    allow:
      - dependency-type: "direct"
      - dependency-type: "indirect"

  # Update for Ruby gems
  - package-ecosystem: "bundler"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    commit-message:
      prefix: "deps"
    allow:
      - dependency-type: "direct"
      - dependency-type: "indirect"

  # Update for Cargo packages
  - package-ecosystem: "cargo"
    directory: "/"
    schedule:
      interval: "daily"
    open-pull-requests-limit: 10
    target-branch: "main"
    labels:
      - "dependencies"
      - "security"
    commit-message:
      prefix: "chore"
      prefix-development: "chore(dev)"
      include: "scope"
    ignore:
      # Ignore updates to sqlx until we resolve the rsa vulnerability
      - dependency-name: "sqlx"
        update-types: ["version-update:semver-patch"]
    
  # Also watch GitHub Actions versions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "ci"
      - "dependencies"

# Optional: Configure automated security updates separately
security-updates:
  # Enable automatic security updates
  enabled: true
>>>>>>> 921251a (fetch)
