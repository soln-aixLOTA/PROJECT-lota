[package]
name = "auth-fuzz-minimal"
version = "0.1.0"
edition = "2021"
description = "Minimal fuzz testing crate for LotaBots auth service"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
proptest = "1.0"

[workspace]
