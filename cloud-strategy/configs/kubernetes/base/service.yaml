apiVersion: v1
kind: Service
metadata:
  name: example-app
  namespace: cloud-strategy
  labels:
    app: example-app
    environment: ${ENVIRONMENT}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: example-app 